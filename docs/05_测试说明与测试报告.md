# 测试说明与测试报告

## 1. 测试目标

验证核心 API 接口正确响应，前端三个视图可正常访问、交互符合需求，移动端布局正确切换，主要边界条件（缺字段、非法参数、重复标签）按预期报错。

## 2. 测试范围

### 已覆盖

- 后端所有 REST 接口（curl 手工验证）
- 前端三级视图基本渲染
- 计划 CRUD 完整流程
- 标签查询与创建
- 缺少必填字段的错误响应
- 移动端布局切换（Chrome DevTools 模拟）
- 代理转发正确性

### 未覆盖

- 单元测试（Service 层 / Repository 层）
- 前端组件自动化测试
- 并发写入压力测试
- 跨浏览器兼容性（仅验证 Chrome）
- 生产构建产物验证

## 3. 测试策略

| 测试类型 | 目标 | 工具 / 方法 |
|---|---|---|
| 接口测试 | 验证后端各端点输入输出正确 | curl 命令行 |
| 页面验证 | 验证三个视图可访问、数据正确展示 | 浏览器手工访问 |
| 边界验证 | 缺字段、非法参数、重复数据的错误处理 | curl + 前端表单操作 |
| 移动端验证 | 768px 断点布局切换正确 | Chrome DevTools 设备模拟 |
| 代理验证 | 前端 /api 请求正确转发到后端 | curl 访问 :8000/api/\* |

## 4. 核心测试用例

| 用例 | 前置条件 | 操作 | 预期结果 | 实际结果 |
|---|---|---|---|---|
| 获取预置标签 | 后端启动，seed 已执行 | GET /api/tags | 返回 5 个预置标签（工作/学习/健身/生活/其他） | ✅ 符合预期 |
| 月视图查询（无数据） | 后端启动 | GET /api/plans?view=month&date=2026-06 | `{"success":true,"data":[]}` | ✅ 符合预期 |
| 年视图查询 | 后端启动 | GET /api/plans?view=year&date=2026 | 返回 YearSummary 数组 | ✅ 符合预期 |
| 创建计划 | 后端启动 | POST /api/plans（含必填字段） | 返回 201 + Plan 对象 | ✅ 符合预期 |
| 创建计划（缺标题） | 后端启动 | POST /api/plans（不含 title） | 400 + INVALID_PARAM | ✅ 符合预期 |
| 更新计划完成状态 | 计划已存在 | PUT /api/plans/:id `{"done":true}` | 返回 200 + 更新后 Plan | ✅ 符合预期 |
| 删除计划 | 计划已存在 | DELETE /api/plans/:id | 返回 200 + `{"id":N}` | ✅ 符合预期 |
| 删除预置标签 | 后端启动 | DELETE /api/tags/1 | 403 + TAG_PRESET_READONLY | ✅ 符合预期 |
| 创建重复标签 | 标签已存在 | POST /api/tags `{"name":"工作"}` | 409 + TAG_ALREADY_EXISTS | ✅ 符合预期 |
| 前端代理转发 | 前后端均启动 | curl :8000/api/tags | 200 + 标签列表（同直连后端） | ✅ 符合预期（需代理目标为 127.0.0.1） |
| 月视图移动端布局 | 前端启动 | Chrome DevTools 切换为手机尺寸 | 日历隐藏，显示日期分组列表 | ✅ 符合预期 |
| 表单必填校验 | 前端启动 | 不填标题直接提交 | 标题字段下方显示错误提示 | ✅ 符合预期 |

## 5. 测试执行结果

```bash
# 获取标签
$ curl -s "http://127.0.0.1:3001/api/tags"
{"success":true,"data":[{"id":1,"name":"工作","is_preset":true},{"id":2,"name":"学习","is_preset":true},{"id":3,"name":"健身","is_preset":true},{"id":4,"name":"生活","is_preset":true},{"id":5,"name":"其他","is_preset":true}]}

# 月视图查询
$ curl -s "http://127.0.0.1:3001/api/plans?view=month&date=2026-06"
{"success":true,"data":[]}

# 年视图查询
$ curl -s "http://127.0.0.1:3001/api/plans?view=year&date=2026"
{"success":true,"data":[]}

# 创建计划
$ curl -s -X POST http://127.0.0.1:3001/api/plans \
  -H "Content-Type: application/json" \
  -d '{"title":"测试计划","date":"2026-06-29","tags":["工作"],"done":false}'
{"success":true,"data":{"id":1,"title":"测试计划","date":"2026-06-29",...}}

# 缺少必填字段
$ curl -s -X POST http://127.0.0.1:3001/api/plans \
  -H "Content-Type: application/json" -d '{}'
{"success":false,"error":{"code":"INVALID_PARAM","message":"Required"}}

# 前端代理（目标改为 127.0.0.1 后）
$ curl -s -w "\nHTTP:%{http_code}" "http://127.0.0.1:8000/api/plans?view=month&date=2026-06"
{"success":true,"data":[]}
HTTP:200
```

## 6. 问题与修复记录

| 问题 | 发现方式 | 原因 | 修复方式 | 复验结果 |
|---|---|---|---|---|
| 后端启动报"目录不存在" | 首次运行 tsx | `getDb()` 在 `mkdirSync` 前被调用，data/ 目录不存在 | 将 `mkdirSync` 移入 `connection.ts` 的 `getDb()` 中，首次打开前自动创建 | ✅ 已修复 |
| 前端出现双层导航栏 | 浏览器访问 | Umi 4 自动将 `src/layouts/index.tsx` 注册为全局布局，路由配置中又手动声明了 component，导致两次渲染 | 路由配置中去掉手动的 layout 包装，交由 Umi 约定机制处理 | ✅ 已修复 |
| 前端代理 /api 返回 404 | curl 访问 :8000 | nvm-windows 旧进程残留，占用 `::1`（IPv6）3001 端口；Umi 代理用 `localhost` 解析到 IPv6，命中无路由的旧进程 | 代理目标从 `localhost` 改为 `127.0.0.1`，强制走 IPv4 | ✅ 已修复 |
| pnpm 安装 better-sqlite3 失败 | 安装时报 node-gyp 错误 | bash wrapper 拦截 pnpm install，内部 node-gyp 使用 Node 16 编译原生模块 | 直接用 `/c/dev/nodejs/node.exe` 运行 pnpm；package.json 添加 `onlyBuiltDependencies` | ✅ 已修复 |
| Umi 配置 antd/layout 报错 | umi setup 输出 | plain `umi` 不支持 `antd`、`layout` 配置项，这是 `@umijs/max` 特有配置 | 从 .umirc.ts 移除这两个配置项 | ✅ 已修复 |

## 7. 交付证据

- 后端接口均返回 `{"success":true,...}` 正确响应（见第 5 节命令输出）
- 前端 http://localhost:8000 可正常访问，三级视图路由均可切换
- 代理 /api 请求成功转发并返回数据
- 移动端 768px 月视图切换为列表模式（Chrome DevTools 验证）
- 所有边界错误均返回预期 error code

## 8. 测试结论

当前版本达到可演示状态。核心 CRUD 流程完整，三级视图正常，代理转发正常，主要边界条件处理符合预期。主要风险：前端无自动化测试，UI 回归依赖手工验收；生产构建产物尚未验证。
